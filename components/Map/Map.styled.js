import styled from "styled-components";

const Map = styled.div`
  // padding: 2rem 0;
  position:relative;
  filter: brightness(0.9);
  height:50vh;

  iframe {
    border: none;
  }
`;

export { Map };
